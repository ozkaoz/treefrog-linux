# Test run: r36sx — 2026-09-10 13:10 (test #7: STOCK + TreeFrogUI v1.0.15 sobre tarjeta B reformateada)

## Contexto

Test #6 cerró la bisección: **k1 (el único kernel nuestro con boot demostrado, test #1)
congela también en la tarjeta B** → los kernels k1/k2 quedan exculpados; el culpable
de los tests #2–#6 es el MEDIO (tarjeta B: 60 GB, FAT32 clúster 32 KB, partición
IsActive=False, o incompatibilidad de la tarjeta en sí).

El usuario reformateó la tarjeta B e instaló **TreeFrogUI v1.0.15 limpio** (13:04).
La instalación trajo la triada de boot **STOCK completa** (verificado por hashes):

| Archivo | SHA256 | Identidad |
|---|---|---|
| cubegm/vmlinux.uImage | `53b3e0b3...` | **STOCK golden** (entry 0x803337c0, 3.9 MB) |
| cubegm/avp.uImage | `a9788995...` | STOCK |
| cubegm/dtb.bin | `1258f1eb...` | STOCK byte-idéntico |

Este estado es por sí mismo el **test #7**: kernel 100% stock sobre la FAT nueva de la
tarjeta B. Una sola variable respecto al boot conocido-bueno: el medio.

| Test | Kernel | Medio | Resultado |
|---|---|---|---|
| #1 19:34 | k1 nuestro | tarjeta A (stock original) | **BOOT OK** |
| #2–#5 | k2 nuestro | tarjeta B (FAT 32K/inactiva) | congela / 0 escrituras |
| #6 | k1 nuestro | tarjeta B (misma FAT) | **congela → medio culpable** |
| **#7 este** | **STOCK** | **tarjeta B reformateada** | **PENDIENTE** |

## Qué se hizo

1. Forense test #6 (logo quieto, sin logs nuevos — coherente con muerte pre-userland
   o FAT no escribible por Linux/hcboot en esa geometría).
2. Usuario: formateo físico de la tarjeta B + instalación limpia TreeFrogUI v1.0.15.
3. Verificación desde WSL: triada stock por hashes (arriba), 4675 archivos, 1.2 GB,
   geometría FAT32 clúster 32 KB (stat -f), `System Volume Information` 13:04 (fresh).
4. NOTAS-PRUEBA.txt v7 en la SD (no toca ningún archivo del sistema).

## Criterio de éxito

- **BOOT OK (esperado):** logo → menú TreeFrogUI v1.0.15 navegable → la tarjeta B
  reformateada queda VALIDADA como medio → siguiente test #8 = deploy de k2
  (joydev, `fe16c9c4`) sobre esta misma FAT = el test decisivo del proyecto
  (kernel nuestro + input funcional).
- **FALLO:** la tarjeta B es incompatible con hcboot a nivel tarjeta (no de geometría
  reformateable) → recuperar la tarjeta A original del test #1 (paradero por confirmar)
  o probar tercera tarjeta.

## Tras la prueba (reinsertar SD en PC)

- Capturar `log.txt`, `tfhijack.log`, `picoarch_init.log` = **baseline de runtime K0
  sobre esta FAT** (procesos, /proc/fb, CPU policy, path del boot) para docs/research-log.
- Si boot OK: procede deploy k2 (test #8) con deploy-sd.sh (backup + verificación).

## Rollback

- No aplica: todo stock, ningún archivo sustituido por el proyecto en este test.

## Resultado

- (pendiente de prueba física — usuario)

## Boot log

- (pendiente)
