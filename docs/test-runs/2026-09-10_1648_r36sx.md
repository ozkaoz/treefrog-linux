# Test run: r36sx — 2026-09-10 16:48 (test #15: k9 = cpio quirúrgico stock + /init nuestro)

## Contexto — R36: el cpio regenerado era el asesino

Test #14 (k8): /init correcto instalado (verificado en cpio embebido), script
sin CRLF, sin UTF-8 en shebang — y **también murió sin escribir nada**. Con el
kernel byte-idéntico al golden k4, la única variable que queda entre
"k4-bootEA" y "k5-k8-mueren" es el **cpio regenerado** (`find | cpio -o` con
uid 1000 en ".", orden de entradas distinto, S-scripts/inittab borrados que
cambian todo el layout). Algún detalle estructural del cpio regenerado mataba
el unpacking del initramfs en el kernel (silencioso, sin UART).

## Fix: cirugía binaria

`build/treefrog-initramfs-surgical.cpio` (sha `dae5066d...`): el cpio STOCK
byte a byte (el mismo que bootea en k3/k4) con **solo el contenido de /init
reemplazado** por nuestro linuxrc v3 (header con fsize actualizado; mode/uid/
mtime/inode del /init stock preservados; resto 379 entradas intactas).
Verificado: 380 entradas, parse correcto, /init = v3 con P01-P13.
Los S-scripts stock quedan (inofensivos sin /sbin/init; iteración futura puede
retirarlos con cirugía adicional).

## Artefacto k9

- Fragment treefrog ahora apunta al cpio quirúrgico. Kernel sha
  `83b023a261fe5a8a7e90265937e75af9ac1e2b72621465f39cbbda030cb3e99e`,
  entry `0x803e5780`, manifest variant treefrog.
- Deploy 16:48 con backup de k8 en `backups-treelinux/2026-09-10_1648_r36sx/`.

## Criterio de éxito

- Menú navegable + `tf-trace.txt` (P01→P13 + LOOP + iters) = FASE F completa:
  la única delta vs k4-golden es el contenido de /init (nuestro v3).
- Fallo con traza → el paso exacto dentro de v3 (fix quirúrgico del script).
- Fallo sin traza → muerte pre-P01 dentro de v3 (primeras líneas) — el stock
  /init era 462 B (3 líneas); se reducirá v3 a mínimo y se re-expandirá.

## Resultado

- (pendiente de prueba física — usuario)

## Boot log

- (pendiente: /mnt/g/tf-trace.txt)
