# Test run: r36sx — 2026-09-10 16:30 (test #14: k8 = /init propio, root cause /init)

## Contexto — ROOT CAUSE de los tests #11/#12/#13 (R35)

Test #13 (k7, traza por pasos): mismo síntoma y **tf-trace.txt NO existía** →
muerte pre-P06... pero eso forzó a cuestionar el supuesto base. Verificación
contra el CÓDIGO del kernel (downloads/linux-4.4.186/init/main.c):

- Línea 1023-1027: con initramfs, `ramdisk_execute_command = "/init"`; si
  `/init` NO existe en el ramdisk → NULL → `prepare_namespace()` (monta rootfs
  REAL de root=) y **el `init=` de bootargs NO se evalúa en ese path** →
  pánico silencioso (sin UART no hay salida visible).
- El initramfs stock lleva `/init` (script: devtmpfs + exec /sbin/init).
- **Nuestro build-initramfs.sh BORRABA /init** (paso 2: rm init) y solo
  instalaba /linuxrc → el kernel NUNCA ejecutó ninguno de nuestros linuxrc
  v1/v2/v3. k5/k6/k7 murieron idénticamente SIN traza (consistente con todos
  los forenses: 0 escrituras, logo AVP eterno).

## Fix

`scripts/build-initramfs.sh`: instalar el linuxrc v3 como **/init** (+ alias
/linuxrc). Verificado en el cpio embebido del build (`usr/initramfs_data.cpio.gz`
mtime 16:29, extracción: /init 6425 B = script v3 con P01-P13, y /linuxrc igual).

## Artefacto k8

- `out/r36sx/vmlinux.uImage` sha `9ded9df9a92c4ea7dc046a3aad6dfca93c4b7281b643cde07fd4166886698c72`,
  entry `0x803e5780` (kernel idéntico al golden k4), manifest variant treefrog,
  cpio `2e48fb67...` (372 entradas).
- Deploy 16:30 con backup de k7 en `backups-treelinux/2026-09-10_1630_r36sx/`.

## Criterio de éxito

- Menú TreeFrogUI navegable = FASE F completa: kernel + init propios.
- `tf-trace.txt` en SD con P01→P13 + LOOP + iteraciones (evidencia runtime).
- Si algo aún falla: la traza por pasos (v3) señala el paso exacto — el
  mecanismo de diagnóstico ya está embebido.

## Resultado

- (pendiente de prueba física — usuario)

## Boot log

- (pendiente: /mnt/g/tf-trace.txt)
