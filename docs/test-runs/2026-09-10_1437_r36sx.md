# Test run: r36sx — 2026-09-10 14:37 (test #9: k3 = initramfs stock embebido + joydev)

## Contexto — ROOT CAUSE de todos los freezers (R30)

Forense binario del stock golden (`53b3e0b3`): su payload raw contiene un
**initramfs CPIO newc embebido de 3.85 MB, 380 archivos** (init→/sbin/init,
linuxrc→busybox, bind-mounts del rootfs en SD: `mount --bind /media/.../rootfs/*`).
El DTS exige `root=/dev/ram0 rootfstype=ramfs rw init=/linuxrc`.

- k1/k2 (base kernel-squashfs.config): `CONFIG_INITRAMFS_SOURCE` vacío →
  **sin raíz** → el kernel muere en init → 0 escrituras en SD → logo eterno.
- Los tests #2/#5/#6/#8 con 0 escrituras son consistentes con esta causa única.
- **Corrección de interpretación (importante):** el "BOOT OK" del test #1 nunca
  existió — el log.txt de esa SD (backup 21:39) era un log **stale de boot stock
  previo** (el zhijack rota log.txt→log.txt.prev al arranque; el contenido que
  atribuimos a k1 era del boot stock anterior). k1 nunca booteó. NO hay ningún
  boot confirmado con kernel nuestro hasta la fecha. El test #1 solo probó que
  hcboot ACEPTA cargar nuestro uImage (logo eterno ≠ rechazo de formato).
- Evidencia de extracción: `scripts/extract-stock-initramfs.sh` (cpio newc parser,
  mayor cpio válido = 380 archivos, sha `deb48ce7...`), reproduce el esquema del
  SDK `kernel-initramfs.config` (CONFIG_INITRAMFS_SOURCE=rootfs.cpio).

## Artefacto k3

- Variante `ramfs` (nueva en build-kernel.sh): base kernel-squashfs.config +
  `boards/r36sx/config/r36sx-ramfs.fragment.config` (joydev + INITRD +
  INITRAMFS_SOURCE=cpio stock). Verificación post-merge: 6/6 opciones activas.
- `out/r36sx/vmlinux.uImage` sha `795b9da4...` 4353642 B (stock: 3905970),
  entry `0x803e4ee0`, load 0x80000000, DTB byte-idéntico stock (`1258f1eb`),
  manifest `config_variant: ramfs`, commit abc0595.
- initramfs embebido: cpio **byte-exacto del stock** (sha `deb48ce7`).
- Deploy con deploy-sd.sh: backup previo (k2 `fe16c9c4`) en
  `backups-treelinux/2026-09-10_1437_r36sx/` de la SD; checksums verificados.

## Criterio de éxito (este es el test de fidelidad máxima al stock)

- **MILESTONE:** menú TreeFrogUI navegable con botones = primer kernel nuestro
  funcional completo (boot scheme stock + input).
- **Fallo con escrituras nuevas** (log.txt rota): el kernel arrancó userland pero
  algo del config nuestro difiere → diagnosticable por logs (¡progreso igualmente!).
- **Fallo con 0 escrituras:** el delta restante es pre-userland (entry/alloca del
  kernel vs stock, o config no-inferible) → siguiente: consola serie virtual
  (frogshell/realterm tfusbhost, backups LPTRACKER 20260910).

## Resultado

- **MILESTONE — PRIMER BOOT FÍSICO CON KERNEL NUESTRO (2026-09-10, reporte usuario):**
  TreeFrogUI inicia correctamente y **es navegable con los botones** (joydev OK:
  cubevol → /dev/input/js0 → /tmp/joy_key → picoarch/FrogUI). El initramfs stock
  embebido resolvió el arranque (R30); la FAT validada (test #7) sostuvo el sistema.
- **Fallo residual:** a los ~7 min aparece "batería agotándose" y la consola se
  apaga — incluso con batería puesta y cargando por OTG. Root cause (R31):
  `CONFIG_CHECK_ADC` ausente en nuestro config (default n del SDK) → sin
  `/dev/check_adc1/5` → `driver.so` del stock lee 0% → warning + poweroff.
- Runtime estable confirmado: 24001+ frames del menú (vs 9001 stock test #7),
  navegación funcional durante toda la sesión.

## Boot log

- `docs/test-runs/log-2026-09-10_k3-firstboot.txt` (9.6 KB, sha 657a6b26) —
  zhijack: procesos boot k3 (init, kworkers, rpcwork, hcdaemon, zhijack, cubevol,
  picoarch pid 1129), fb/hwdisp/audio init OK, 24001+ retro_run frames.
- `tfhijack-2026-09-10_k3.txt`, `picoarch-2026-09-10_k3.txt`,
  `frogui-crash-2026-09-10_k3.txt` (retro_init complete OK).

## Síntomas / siguiente acción

- k4 = k3 + `CONFIG_CHECK_ADC=y` (test #10) → batería legible → sin apagado.
- Tras k4: FASE E bring-up formal (poweroff/reboot limpio, audio/AVP, USB) con
  dmesg/proc capturable por zhijack; luego FASE F (initramfs TreeFrog propio).
