# Test run: r36sx — 2026-09-10 13:17 (test #8: DECISIVO — k2 JOYDEV sobre FAT validada)

## Contexto

Test #7 = **BOOT OK** con 100% stock sobre la tarjeta B reformateada + TreeFrogUI
v1.0.15 → medio VALIDADO (los logs del boot stock quedaron escritos en la SD =
FAT escribible por Linux). Baseline runtime stock capturado y preservado en
`docs/test-runs/log-2026-09-10_stock-fatk2.txt` + `tfhijack-2026-09-10_stock-fat.txt`
(procesos boot: picoarch pid 573, /proc/fb `0 hcfb / 1 hcfb`, fb0 720x1280@32bpp
smem 0xaf92000, hwdisp driver_r36sx.so, audio 44100→48000, panel 640x480).

Matriz completa:

| Test | Kernel | Medio | Resultado |
|---|---|---|---|
| #1 | k1 nuestro `875854cb` | tarjeta A stock | **BOOT OK** (input muerto) |
| #2–#5 | k2 nuestro | tarjeta B FAT vieja | congela, 0 escrituras |
| #6 | k1 nuestro | tarjeta B FAT vieja | congela → MEDIO culpable |
| #7 | STOCK `53b3e0b3` | tarjeta B reformateada | **BOOT OK** → medio validado |
| **#8 este** | **k2 `fe16c9c4` (joydev)** | **tarjeta B validada** | **PENDIENTE — decisivo** |

## Artefactos desplegados (deploy-sd.sh --apply)

- commit probado (repo al momento del deploy): `f3b4ce5` (build del kernel: ac04c73)
- board: r36sx
- Kernel: k2 `fe16c9c45e2553cfb3d3a2003feccbce0667f0ea1e5b98983cd08f99f8d45c58`
  (2702876 B; Linux 4.4.186 + 41 patches + hcdrivers + CONFIG_INPUT_JOYDEV=y,
  entry 0x803e4930, load 0x80000000). Diferencia vs k1 (boot probado): SOLO joydev —
  verificado con diff de configs efectivos (R27).
- dtb.bin `1258f1eba809e43540c581b815c87815540a9e5897a4e8584363ab7de5cc27bb`
  (33137 B, byte-idéntico stock) — verificado post-copia.
- avp.uImage `a9788995` stock — NO tocado.
- archivos sustituidos en SD: `cubegm/vmlinux.uImage`, `cubegm/dtb.bin`
- backup previo en SD: `backups-treelinux/2026-09-10_1317_r36sx/` (stock golden
  `53b3e0b3` + dtb + avp, con SHA256SUMS). Rollback = restaurar esos archivos a
  `cubegm/` y sync.

## Criterio de éxito

- **MILESTONE:** menú TreeFrogUI navegable con botones (joydev → /dev/input/js0 →
  cubevol → /tmp/joy_key → picoarch/FrogUI) = primer kernel nuestro 100% funcional
  = base Linux TreeFrog consolidada (FASE D completa).
- **Fallo:** logo quieto → k2 muere donde k1 no — con medio exculpado (test #7) y
  delta k1↔k2 = solo joydev, siguiente paso = consola serie virtual (zhijack +
  frogshell realterm, backups LPTRACKER 20260910) para diagnóstico en vivo.

## Tras la prueba

- Reinsertar SD → diff de logs vs baseline stock (`log-2026-09-10_stock-fatk2.txt`):
  navegación = cambio de core frogui→X en game_history/log, eventos js0.

## Resultado

- (pendiente de prueba física — usuario)

## Boot log

- (pendiente)

## Síntomas / siguiente acción

- (pendiente)
