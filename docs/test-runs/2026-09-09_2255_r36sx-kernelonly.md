# Test run: r36sx — 2026-09-09 22:55 (kernel-only: SD limpia + initramfs propio)

## Experimento (diseño acordado con el usuario)

Prueba PURA sin resagos: SD formateada lógicamente (borrado total) + SOLO nuestro
kernel. Sin TreeFrogUI, sin SO stock, sin rkgame, sin rootfs externo.

| Archivo en SD | Qué es |
|---|---|
| `cubegm/vmlinux.uImage` | **Nuestro kernel kernelonly**: 4.4.186 + 41 patches + linux-drivers + **initramfs propio** (tfinit estático escrito por nosotros, compilado con el toolchain del fabricante) + **DTB stock embebido** (.appended_dtb, byte-exacto). sha `465d5fdb...` |
| `cubegm/avp.uImage` | AVP stock (estrategia FASE 1: el AVP gestiona display/audio del SoC; el plan del proyecto lo conserva) |
| `cubegm/dtf.bin` (dtb.bin) | stock byte-identico (por si hcboot lo solicita) |

### El initramfs (tfinit)

Escrito desde cero (`rootfs/tfinit/tfinit.c`), estático MIPS32r2 LE:
1. Escribe banner en /dev/console (y ttyS0)
2. Abre /dev/fb0 y /dev/fb1 (mmap + ioctls), reporta geometría
3. **Pinta BARRAS DE COLOR en pantalla** (patrón test RGB) — prueba visible de kernel+fb
4. Sondea /dev/input/js0..3 y event0, cuenta eventos que fluyen (prueba input)
5. Bucle eterno: barras parpadeantes + heartbeat por consola + re-sondeo input

## Resultado

- (PENDIENTE DE PRUEBA FÍSICA)

## Qué esperar

- **Barras de color en pantalla** → kernel nuestro vivo, framebuffer OK; el resto
  (input reports) por consola serie si hay acceso.
- **Logo quieto (sin barras)** → el kernel muere antes del init: candidate causes:
  (a) FAT corrupto persistente → formateo físico FAT32 de la SD (usuario) y recopia;
  (b) el payload con DTB+initramfs embebidos confunde a hcboot (tamaño/entry);
      siguiente iteración: probar uImage SIN DTB embebido + dtb.bin aparte (modo stock).
- **Pantalla negra tras logo** → kernel arrancó pero fbcon/fb0 difiere: iterar con
  earlycon por UART (el bootargs del DTB stock usa earlycon=).

## Artefactos

- Kernel: manifest out/r36sx (config_variant kernelonly), commit 0fbd38b + fragment
  `28c0f92d...` (INITRD+ELF_APPENDED_DTB+FB_CONSOLE+JOYDEV), initramfs `fa0b3893...`
  (cpio.gz 279055 B), vmlinux.bin 6192901 B con DTB embebido verificado (0x5dfd94,
  33137 B == stock), uImage `465d5fdb...` entry 0x803ec710.
- SD: borrado total + 3 archivos (verificados por sha256 post-copia).
- Backup completo previo intacto: D:\R36SX\sd-full-backups\2026-09-09_2139_treefrog-test-sd.

## Boot log

- (pendiente; esta prueba no escribe logs en SD — el tfinit escribe por consola)
