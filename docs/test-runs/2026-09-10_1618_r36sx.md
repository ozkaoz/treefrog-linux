# Test run: r36sx — 2026-09-10 16:18 (test #13: k7 = init v3, forense por pasos)

## Contexto — re-interpretación forense tests #11/#12 (R34)

Test #12 mismo síntoma que #11. Forense comparativo: **NINGÚN log de la SD creció**
en el test #12 (picoarch_init/frogui_crash/tfhijack/log.txt byte-idénticos al fin
del test #11). Conclusión crítica: los bloques pid 1134/ppid 1104 que en su
momento atribuí a k5 eran de la **sesión k4** (ppid 1103 = zhijack de k4).
**Ni k5 ni k6 lanzaron picoarch jamás** — nuestro init moría SIEMPRE antes del
loop, sin escribir nada (ni /tmp→SD). El busybox stock verificado completo
(v1.33, swapon/mkswap/mdev/free presentes). El init STOCK (k4) funcionaba.

Diferencias estructurales init propio vs stock que v3 corrige:
- v3 monta devpts/dev/shm/run (como inittab stock) además de dev/proc/sys/tmp.
- v3 NO depende de mdev para la SD: `mount -t vfat /dev/mmcblk0pN /media/sd1`
  directo (el flujo stock monta via mdev.conf+mount-helper.sh hotplug).
- v3 traza cada paso a /run/trace.txt y vuelca a /mnt/sdcard/tf-trace.txt con
  sync por paso — la muerte deja su huella en la SD.

## Artefacto k7

- linuxrc v3 (rootfs/treefrog/linuxrc): P01 fs-base (con pts/shm/run),
  P02 console, P03 hcdaemon, P04 detectar SDDEV, P05 mount vfat manual,
  P06 bind cubegm, P07 traza→SD, P08 swapon pagefile.sys, P09 binds rootfs
  (lib/usr/bin/sbin/etc COMO S99app COMPLETO — v2 solo montaba lib/usr/lib32),
  P10 mdev, P11 cubevol, P12 cpufreq, P13 env. Loop FrogUI con cap 5 muertes
  (evita escritura infinita a la SD) + tail-5 del stderr de picoarch por iter.
- cpio regenerado (371 entradas), kernel variante treefrog sha
  `c9a4523b...` entry `0x803e5780` (kernel idéntico al golden k4).
- Deploy 16:18, backup k6 en `backups-treelinux/2026-09-10_1618_r36sx/`.

## Criterio de éxito / lectura del resultado

- Menú navegable → tf-trace.txt completo con "=== LOOP frogui" + iter frames.
- Fallo → **tf-trace.txt dice el paso exacto de muerte** (o su ausencia =
  muerte pre-P06: fs-base/mount). Sin ambigüedad posible.

## Resultado

- **FALLO, mismo síntoma — y la traza reveló el bug estructural (R35):**
  `tf-trace.txt` NO existía → muerte pre-P06... lo que forzó a cuestionar el
  supuesto base: 3 linuxrc distintos muriendo sin escribir NADA = el kernel
  NUNCA ejecutó nuestro script. Verificado contra el código del kernel:
  con initramfs, el kernel ejecuta **/init** (init/main.c:1023-1027); si no
  existe → prepare_namespace() (rootfs real) → `init=/linuxrc` de bootargs
  JAMÁS se evalúa → pánico silencioso. Nuestro build BORRABA /init del cpio
  stock y solo instalaba /linuxrc → kernel sin init → logo eterno del AVP.

## Boot log

- (sin traza: el kernel nunca llegó a userland nuestro — R35)

## Síntomas / siguiente acción

- k8 (test #14): build-initramfs.sh instala nuestro linuxrc v3 COMO /init
  (+ alias /linuxrc). Verificado en el cpio embebido. Ver
  `docs/test-runs/2026-09-10_1630_r36sx.md`.
