# Test run: r36sx — 2026-09-10 13:00 (test #6: BISECCIÓN k1 sobre tarjeta B)

## Contexto

Test #5 (kernel k2 JOYDEV `fe16c9c4`, sistema completo restaurado sobre la tarjeta B
de 60 GB) resultó en **logo quieto/congelado**. Forense post-prueba (este archivo):

- Los 5 logs de la SD (`log.txt`, `tfhijack.log`, `picoarch_init.log`, `frogui_crash.log`,
  `game_history.txt`) son **byte-idénticos al backup 21:39** → zhijack nunca ejecutó
  ni su primer paso (rotación de log.txt) → el userland nunca corrió (o no pudo
  montar/escribir la FAT de la tarjeta B).
- El único archivo modificado en la SD desde el deploy: `cubegm/vmlinux.uImage`
  (verificado). Restauración íntegra: 4693 archivos = backup 21:39 + NOTAS-PRUEBA.txt.
- k2 descomprime limpio (gunzip stream completo OK, 5812924 B raw); diff de configs
  efectivos k1 vs k2 (reproducido localmente con merge_config + olddefconfig) =
  **exactamente 1 línea: CONFIG_INPUT_JOYDEV=y**. No hay drift de config oculto.
- dumpimage -l OK en k1/k2/stock (CRC uImage válidos, mismo formato/load/entry-style).

Matriz de tests física:

| Test | Kernel | Tarjeta | Resultado |
|---|---|---|---|
| #1 19:34 | k1 `875854cb` (sin joydev) | A (original stock) | **BOOT completo** (FrogUI 4200+ frames, input muerto) |
| #2 21:54 | k2v1 `9f5d3f9c` | A (FAT ya corrupta) | congela, 0 escrituras + archivo basura `%░Ç` |
| #3/#4 | kernel-only initramfs | B (60GB, clúster 32K, IsActive=False) | `Please insert TF card` |
| #5 02:30 | k2v2 `fe16c9c4` | B | congela, **0 escrituras** |
| **#6 este** | **k1 `875854cb`** | **B** | **PENDIENTE** |

## Qué se hizo

1. Forense test #5 (evidencia arriba).
2. Copia de **k1** (el único kernel nuestro con boot físico demostrado, test #1) sobre
   `cubegm/vmlinux.uImage` de la tarjeta B. SHA256 verificado post-copia:
   `875854cbba847018c2f72b8fc8b128e7785085dc98641034a204cc30003f0484`.
   dtb.bin (`1258f1eb`) y avp.uImage (`a9788995`) stock NO tocados.
3. NOTAS-PRUEBA.txt v6 en la SD explicando la prueba.

Este test **aísla la variable** que el test #5 dejaba confundida:

- Si k1 boota en la tarjeta B (logo → ~30-60 s → menú TreeFrogUI; input muerto es
  **esperado** en k1) → el culpable del test #5 es el build k2/joydev → siguiente paso:
  serial console (bootargs `console=tty1` no da log por UART) o bisección del build.
- Si k1 TAMBIÉN congela en la tarjeta B → el problema es la tarjeta B (geometría
  FAT32 clúster 32K / partición IsActive=False / compatibilidad de la tarjeta de 60 GB)
  → recuperar la tarjeta A original del test #1 o reformatear B (con backup previo).

## Artefactos

- Kernel probado: k1 = commit 81ecf9a, uImage sha `875854cb...`, entry `0x803e3200`
  (fuente: `D:\R36SX\sd-full-backups\2026-09-09_2139_treefrog-test-sd\cubegm\vmlinux.uImage`)
- Backup previo del archivo sustituido: el k2 copiado está en `out/r36sx/vmlinux.uImage`
  (`fe16c9c4...`) y en el registro del test #5.
- Rollback kernel stock: `boards/r36sx` golden `53b3e0b3`
  (`/mnt/d/GitHub/lgpt-r36sx-port/physical-evidence/stock-kernel-golden/vmlinux.uImage.stock`).

## Resultado

- (pendiente de prueba física — usuario)

## Boot log

- (pendiente — tras la prueba, diff de logs SD vs backup 21:39)

## Síntomas / siguiente acción

- Boot completo con logo quieto→menú (input muerto): k2 es el culpable → re-test con
  k2 recortado (solo joydev) tras serial console.
- Logo quieto 2+ min sin avanzar: tarjeta B es el culpable → recuperar tarjeta A o
  reformatear B (clúster 4K + activa, con backup).
