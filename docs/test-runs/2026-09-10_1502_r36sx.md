# Test run: r36sx — 2026-09-10 15:02 (test #10: k4 = k3 + CONFIG_CHECK_ADC, fix batería)

## Contexto

Test #9 = MILESTONE (primer boot propio, TreeFrogUI navegable) con fallo residual:
"batería agotándose" + apagado a ~7 min con batería puesta y OTG conectado.

Root cause (R31): `CONFIG_CHECK_ADC` (default n en el SDK) → sin driver
`hc16xx-check-adc` → sin `/dev/check_adc1/check_adc5` (nivel/cargando, según DTS
`adc-bat-level`/`adc-bat-charging`) → `driver.so` del stock lee 0% → warning +
poweroff. Stock lo lleva (strings `hc16xx-check-adc`, `check_adc%d`).

## Artefacto k4

- Fragment `r36sx-ramfs.fragment.config` ampliado: `CONFIG_CHECK_ADC=y` (además
  de JOYDEV + INITRD/INITRAMFS_SOURCE del cpio stock). Verificación post-merge 7/7.
- `out/r36sx/vmlinux.uImage` sha `2dda38abdf97d620814a86bdd38e0ea1dd9b9a5076c9e3bcfc8b29fc6533bb8b`,
  4355225 B, entry `0x803e5780`, config_variant ramfs, DTB byte-idéntico stock.
- Verificación binaria: strings de k4 contienen `hc16xx-check-adc`/`check_adc%d`.
- Deploy 15:02 con backup de k3 (boot OK) en `backups-treelinux/2026-09-10_1502_r36sx/`.

## Criterio de éxito

- Menú navegable (como test #9) + **sin mensaje de batería ni apagado**; sesión
  estable indefinida con batería/carga OTG.
- Bonus esperado: icono/porcentaje de batería real en el menú (driver.so ahora
  puede leer check_adc1/5).

## Resultado

- (pendiente de prueba física — usuario)

## Boot log

- (pendiente — tras la prueba)

## Síntomas / siguiente acción

- OK → FASE E formal: poweroff/reboot limpios, audio/AVP, USB, dmesg runtime
  (via zhijack), y FASE F (initramfs TreeFrog propio).
- Persiste el aviso → calibración ADC distinta: capturar dmesg y lecturas
  check_adc desde consola (zhijack/frogshell) para comparar contra stock.
